<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Beautyfresh"
        tools:targetApi="31">
        <activity android:name=".UserAdmin"></activity>
        <activity android:name=".AdminLogin" />
        <activity android:name=".Register" />
        <activity android:name=".EditProfile" />
        <activity android:name=".Login" />
        <activity android:name=".Home"/>
        <activity android:name=".viewCalculatiopn" />
        <activity android:name=".ViewShoppingCart" />
        <activity android:name=".MainAdmin" />
        <activity android:name=".AddToShoppingCart" />
        <activity android:name=".SucessPayment" />
        <activity android:name=".UpdateDeleteShipping" />
        <activity android:name=".AddPaymentDetails" />
        <activity android:name=".AddShippingDetails" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>